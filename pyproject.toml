[project]
name = "protected-parser"
version = "0.1.0"
description = "Steam UserGameStatsSchema parser for finding protected achievements."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "sortedcontainers>=2.4.0",
    "vdf>=3.4",
]
